import 'package:flutter/material.dart';

const Color KgrayColor = Colors.grey;
const Color KGreenColor = Colors.green;

const headingTextStyle =
    TextStyle(fontSize: 30, color: KgrayColor, fontWeight: FontWeight.w500);
